name: WakaTime Dev Metrics

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-readme-stats:
    runs-on: ubuntu-latest
    
    env:
      TZ: Asia/Shanghai # 确保 Runner 系统时间为北京时间
      
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run WakaTime Stats Action
        uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          
          SECTION_NAME: waka
          # 🔒 隐私与范围设置
          SHOW_PROJECTS: false
          SHOW_TIMEZONE: false
          SHOW_DAYS_OF_WEEK: false
          SHOW_OS: false
          SHOW_EDITORS: true
          
          # 🚀 数据展示设置
          SHOW_LINES_OF_CODE: true
          SHOW_LOC_CHART: true
          SHOW_COMMIT: true
          SHOW_LANGUAGE: true
          SHOW_LANGUAGE_PER_REPO: false
          SHOW_PROFILE_VIEWS: true
          
          # 💡 这里建议设为 true，它会显示“Last 7 days”字样和具体日期范围
          SHOW_SHORT_INFO: true
          
          SHOW_UPDATED_DATE: true
          SHOW_TOTAL_CODE_TIME: true
          
          # 🎨 格式化设置
          COMMIT_MESSAGE: "Updated with Dev Metrics 🎮"
          LOCALE: zh
          # 这样写配合 TZ 环境变量，输出的就是纯正的北京时间啦！
          UPDATED_DATE_FORMAT: '%Y/%m/%d %H:%M:%S'
          UPDATED_DATE_TEXT: "最后更新于"
          SYMBOL_VERSION: 1