name: WakaTime Dev Metrics # 全新的工作流名称

on:
  schedule:
    - cron: '0 0 * * *'      # 每天 00:00 自动更新
  workflow_dispatch:         # 手动触发也可以

jobs:
  update-readme-stats: # 全新的 Job 名称
    runs-on: ubuntu-latest
    
    # 🌟 关键：因为 Secret 已经挪到仓库级，所以这里不需要 environment 字段了！
    
    # 🌟 权限配置：必须保留！用于解决 403 错误（假设你已经修正了 PAT 权限）
    permissions:
      contents: write    # 允许提交更新后的 README 文件
      traffic: read      # 允许读取仓库的流量（views/traffic）数据
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run WakaTime Stats Action
        uses: anmol098/waka-readme-stats@master
        with:
          # 引用仓库级 Secret
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          
          # 以下是你原有的配置
          SECTION_NAME: waka
          SHOW_OS: true
          SHOW_PROJECTS: true
          SHOW_EDITORS: true
          SHOW_TIMEZONE: true
          SHOW_COMMIT: true
          SHOW_LANGUAGE: true
          SHOW_LINES_OF_CODE: false
          SHOW_LANGUAGE_PER_REPO: true
          SHOW_LOC_CHART: true
          SHOW_DAYS_OF_WEEK: true
          SHOW_PROFILE_VIEWS: true
          SHOW_SHORT_INFO: true
          SHOW_UPDATED_DATE: true
          SHOW_TOTAL_CODE_TIME: true
          COMMIT_BY_ME: false
          COMMIT_MESSAGE: Updated with Dev Metrics
          COMMIT_SINGLE: false
          LOCALE: en
          UPDATED_DATE_FORMAT: '%d/%m/%Y %H:%M:%S'
          SYMBOL_VERSION: 1